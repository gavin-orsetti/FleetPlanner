<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:FleetPlanner.MVVM.ViewModels"
             x:Class="FleetPlanner.MainPage"
             Title="My Fleets">

    <ContentPage.BindingContext>
        <viewmodels:TestBed/>
    </ContentPage.BindingContext>
    
    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Label
                Text="My Fleets"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                FontSize="18"
                HorizontalOptions="Center" />

            <Button
                x:Name="GoToFleetBtn"
                Text="Load Ships"
                Command="{Binding LoadShips}"
                SemanticProperties.Hint="Sends you to the fleet page (substitute for a list item atm)"
                HorizontalOptions="Center" />

            <!--<CollectionView ItemsSource="{Binding Ships}">
                
            </CollectionView>-->

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
