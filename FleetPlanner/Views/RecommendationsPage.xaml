<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:FleetPlanner.ViewModels"
             x:Class="FleetPlanner.Views.RecommendationsPage"
             x:DataType="vm:RecommendationsViewModel"
             Title="Recommendations">

    <VerticalStackLayout Padding="16" Spacing="16" VerticalOptions="Center" HorizontalOptions="Center">

        <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}"
                           HorizontalOptions="Center" />

        <Label Text="Recommendations" FontSize="22" FontAttributes="Bold" HorizontalTextAlignment="Center" />

        <Label Text="{Binding StatusMessage}" FontSize="14" TextColor="{StaticResource Gray400}"
               HorizontalTextAlignment="Center" />

        <Label Text="{Binding TotalShips, StringFormat='{0} ships in collection'}"
               FontSize="12" TextColor="{StaticResource Gray500}" HorizontalTextAlignment="Center"
               IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}" />

    </VerticalStackLayout>
</ContentPage>
