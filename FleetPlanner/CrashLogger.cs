namespace FleetPlanner;

/// <summary>
/// Placeholder for a future crash logging / telemetry service.
/// <para>
/// Currently empty. When implemented, this will capture unhandled exceptions and write them to
/// a log file or send them to a crash reporting service (e.g., App Center, Sentry).
/// The Android-specific <c>AndroidCrashHandler</c> in <c>Platforms/Android/</c> is designed
/// to work with this class.
/// </para>
/// </summary>
public static class CrashLogger { }
